import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
	colors: {
		background: '#212121',
		purple: { 900: '#7B61FF' },
		black: { 900: '#333333' },
		gray: { 900: '#909090' },
	},
});

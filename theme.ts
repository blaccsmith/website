import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
	colors: {
		background: '#212121',
		translucent: 'rgba(33, 33, 33, 0.65)',
		brand: { purple: '#7B61FF' },
		black: { 900: '#333333' },
		gray: { 900: '#909090' },
	},
});
